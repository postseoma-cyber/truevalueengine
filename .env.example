# Copy to .env.local and fill in. Never commit the filled-in copy.
DATABASE_URL=postgresql://user:password@host:5432/tve
SITE_URL=https://www.truevalueengine.com
# Set to 1 on any host that is not production: robots.txt then serves
# "Disallow: /". Read at request time, so one image serves both.
TVE_STAGING=
